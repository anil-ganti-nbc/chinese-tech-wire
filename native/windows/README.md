# Chinese Tech Wire Windows packaging

The existing Windows launcher remains defined by `ChineseTechWire.spec` and
`launcher_main.py` at the repository root. This directory records the fleet
layout without moving or duplicating that established packaging path.
